pipeline {
    agent {
    	dockerfile true 
    }
    stages {
        stage('Build') { 
            steps {
		echo 'Hello World!'
                //sh 'npm install' 
            }
        }
    }
}
